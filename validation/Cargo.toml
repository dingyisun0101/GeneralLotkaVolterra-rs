[package]
name = "glv-legacy-validation"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
current-glv = { package = "general-lotka-volterra-rs", path = ".." }
legacy-glv = { package = "general-lotka-volterra-legacy", path = "legacy-source" }
ndarray = "0.17.2"
rand = "0.10"
rand_chacha = "0.10"
serde_json = "1"
