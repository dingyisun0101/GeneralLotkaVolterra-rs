/*!
Executable example selector.

Purpose:
    `main` keeps local runnable examples lightweight. Switch the called example
    here when manually exercising a task configuration.
*/

fn main() {
    // general_lotka_volterra_rs::examples::replicator_deterministic::run();
    general_lotka_volterra_rs::examples::replicator_demographic::run();
}
